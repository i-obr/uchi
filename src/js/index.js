

window.addEventListener('keydown', (event) => {
  if (event.keyCode === 37) {
    console.log('left');
  }
  if (event.keyCode === 40) {
    console.log('down');
  }
  if (event.keyCode === 39) {
    console.log('right');
  }
});
